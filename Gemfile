# A sample Gemfile
source "https://rubygems.org"

# gem "rails"

gem 'sinatra'
gem 'rake'
gem 'thin'
gem 'shotgun'
gem 'pry'
gem 'require_all'
    
#(terminal) bundle install : install gems in Gemfile
#(terminal) gem install shotgun : install shotgun